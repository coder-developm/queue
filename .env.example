DB_DSN=mysql:host=127.0.0.1;dbname=queue;charset=utf8mb4
DB_USER=root
DB_PASS=

# Mini-admin credentials for /admin (to generate /create/<token> links)

# Mini-admin credentials are stored in DB (admin_users).
# First time setup can be done via /admin (setup screen) when no users exist.

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Web Push / VAPID
VAPID_SUBJECT=mailto:admin@example.com
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
